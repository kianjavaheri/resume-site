---
name: feedback_no_push
description: Never push to GitHub — user handles all git pushes themselves
metadata:
  type: feedback
---

Never run `git push` in this project. Make commits locally but stop there.

**Why:** User explicitly asked to handle all GitHub pushes themselves.

**How to apply:** After committing, stop. Do not push. If changes need to be deployed, note that the user will push when ready.
